spring:
  profiles:
    active: ${profiles.active}
  application:
    name: "clerk-oauth"
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
        file-extension: "yml"
