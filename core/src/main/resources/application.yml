logging:
  level:
    root:
      info
mybatis:
  type-aliases-package: com.ps.judge.dao.entity
  configuration:
    map-underscore-to-camel-case: true
server:
  port: 8080

spring:
  application:
    name: clerk